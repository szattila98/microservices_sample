# Name of the application so the client knows which property file to fetch and the config servers uri.
spring:
  application:
    name: employee-service
  cloud:
    config:
      uri: http://localhost:8888